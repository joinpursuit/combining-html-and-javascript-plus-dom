document.addEventListener("DOMContentLoaded", () => {
    
    let enterName = window.prompt("enter")

    let result = alert(`${enterName}`);
    document.querySelector("#name").innerText = string

})